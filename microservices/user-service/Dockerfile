# Dockerfile for user-service
# REQUIRED: Set your base image and add application code
FROM node:18-alpine
WORKDIR /app
COPY . .
# REQUIRED: Add install and build steps for your app
CMD ["node", "index.js"]
# Dockerfile for User Service
# Fill in with your base image, dependencies, and entrypoint
